#pragma once

#include <stdio.h>
#include <stdbool.h>
#include <math.h>

bool analyzeRectangle(double, double);

int* findRectangleCorners(int[]);

double* findRectangleSides(int[]);

void findRectanglePerimeter(double, double);

void findRectangleArea(double, double);